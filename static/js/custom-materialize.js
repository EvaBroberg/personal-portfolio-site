$(document).ready(function () {
    //INIT SIDENAV
    $('.button-collapse').sideNav();

    //INIT SCROLLSPY
    $('.scrollspy').scrollSpy();

    //INIT MODAL
    $('.modal').modal();
          
    Materialize.scrollFire(options);
  });